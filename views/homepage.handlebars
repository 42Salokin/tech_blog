{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{user.username}}</h2>
  </div>
  <div class="col-md-6">
    {{#each user.posts as |post| }}
    <h3>{{post.title}}</h3>
    <p>{{post.body}}</p>
    {{/each}}
  </div>
</div>
{{/each}}


